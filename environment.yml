# conda env create -f environment.yml
name: birdy
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  #- openssl
  - lxml
  - owslib>=0.19
  - click
  - jinja2
  - wrapt
  - funcsigs
  - boltons
  # optional
  - netcdf4
  - xarray
  # tests
  - pytest
  - pytest-notebook
  - nbval
  - flake8>=3.8.4
  # sphinx for rtd
  - pandoc
  - ipykernel
  - sphinx
  - nbsphinx
  - nbconvert
