# conda env create -f environment.yml
name: birdy
channels:
  - birdhouse
  - conda-forge
  - defaults
dependencies:
  - pip
  #- openssl
  - lxml
  - owslib>=0.18
  - owslib-esgfwps=0.1.0
  - click
  - jinja2
  - wrapt
  - funcsigs
  - boltons
  # optional
  - netcdf4
  - xarray
  # tests
  - pytest
  - flake8
  # sphinx for rtd
  - sphinx
  - nbsphinx
  - pandoc
  - ipykernel
  - nbconvert
