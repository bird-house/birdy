# conda env create -f environment.yml
name: birdy
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  #- openssl
  - boltons
  - click
  - funcsigs
  - jinja2
  - lxml
  - owslib>=0.19
  - packaging
  - pyopenssl
  - wrapt
  # optional
  - netcdf4
  - xarray
  # tests
  - flake8>=3.8.4
  - nbval
  - pytest
  - pytest-notebook
  # sphinx for rtd
  - ipykernel
  - nbconvert
  - nbsphinx
  - pandoc
  - sphinx
