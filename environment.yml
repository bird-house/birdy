# conda env create -f environment.yml
name: birdy
channels:
  - birdhouse
  - conda-forge
  - defaults
dependencies:
  - six
  - lxml
  - owslib>=0.17
  - click
  - jinja2
# tests
  - pytest
  - flake8
