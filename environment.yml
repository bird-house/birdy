# conda env create -f environment.yml
name: birdy
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  #- openssl
  - lxml
  - owslib>=0.18
  - click
  - jinja2
  - wrapt
  - funcsigs
  - boltons
  # optional
  - netcdf4
  - xarray
  # tests
  - pytest
  - pytest-notebook
  - nbval
  - flake8
  # sphinx for rtd
  - pandoc
  - ipykernel
  - sphinx
  - nbsphinx
  - nbconvert
